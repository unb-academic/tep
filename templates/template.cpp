#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

auto solve() {}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    ll N = 1;
    cin >> N;

    while (N--)
        solve();

    return 0;
}
